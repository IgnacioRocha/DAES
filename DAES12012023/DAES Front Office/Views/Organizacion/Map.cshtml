@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <title>Simple Map</title>
    <meta name="viewport" content="initial-scale=1.0">
    <meta charset="utf-8">
    <style>
        /* Always set the map height explicitly to define the size of the div
        * element that contains the map. */
        #map {
            height: 100%;
        }
        /* Optional: Makes the sample page fill the window. */
        html, body {
            height: 100%;
            margin: 0;
            padding: 0;
        }
    </style>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueryui")
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBW9a-r2pTB29-ADxw8GKIkX0WOyfVvM9A&callback=initMap" async defer></script>

</head>
<body>
    <div id="map"></div>
    <script>
        var map;
        function initMap() {
            map = new google.maps.Map(document.getElementById('map'), { center: { lat: -33.456174, lng: -70.654267 }, zoom: 12 });
            //$.getJSON("../Organizacion/GetAddress", null,
            //    function (classesData) {
            //        $.each(classesData, function (key, value) {
            //            $.getJSON('https://maps.googleapis.com/maps/api/geocode/json?key=AIzaSyCTpIYNcyx6WAM4KDfyyQ_p-9_cWcc_LKs&address=' + value.Direccion + '&sensor=false', null, function (data) {
            //                var p = data.results[0].geometry.location
            //                var latlng = new google.maps.LatLng(p.lat, p.lng);

            //                var marker = new google.maps.Marker({
            //                    position: latlng,
            //                    map: map
            //                });

            //                var infowindow = new google.maps.InfoWindow({
            //                    content: '<b>' + value.RazonSocial + '</b>'

            //                });

            //                marker.addListener('click', function () {
            //                    infowindow.open(map, marker);
            //                });

            //            });
            //        });
            //    });
        }
    </script>
</body>
</html>