@model DAES.Web.FrontOffice.Controllers.ProcesoController.DtoProceso
<div class="jumbotron">
    <div class="container">
        <h1 class="display-4">Consultar trámite</h1>
        <p class="lead">La emisión de certificados manuales se encuentra condicionada a la disponibilidad de antecedentes para su otorgamiento</p>
    </div>
</div>
<div class="container my-3">
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Búsqueda de trámite</h5>
                <p class="card-title">Ingrese el número del trámite</p>
                <div class="form-group">
                    @Html.EditorFor(m => Model.id, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(m => Model.id, "", new { @class = "text-danger" })
                </div>
                <button class="btn btn-primary btn-rounded" type="submit">Buscar</button>
            </div>
        </div>
    }
</div>
<div class="container my-3">
    Volver al <a href='@Url.Action("Index", "DAES")'>DAES digital</a>
</div>