@model IEnumerable<DAES.Model.SistemaIntegrado.TipoMateria>
@{
    ViewBag.Title = "Tipo de materia";
}

<table class="table table-hover table-striped datatable">
    <thead>
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().TipoMateriaId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().Descripcion)
            </th>
            <th>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.TipoMateriaId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>
                <td>
                    @Html.ActionLink("Detalles", "Details", new { id = item.TipoMateriaId })
                </td>
            </tr>
        }
    </tbody>
</table>

@Html.ActionLink("Crear nuevo", "Create") |
@Html.ActionLink("Volver al inicio", "Index", "Home")